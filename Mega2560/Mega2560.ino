// SD - Version: Latest#include <SD.h>// DS1307RTC - Version: Latest#include <DS1307RTC.h>#include <Wire.h>/** * Main Lawn_Mower**//** *  * #ifndef DEBUG; * #else * #endif**/#define DEBUG; //enable debug mode/** * ATTENTION Global variables and constants used internally are definend into each tab**///Global Variables configurations properties//-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=boolean logEnable = true; //True for log activation//Constant configurations properties//-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=const boolean RTC_ON = true; // On if Real time clock is installed and connectedconst boolean sdCard_ON = true; // On if Real time clock is installed and connectedconst boolean blade_ON = true; //On if blade H-Bridge installed and connected/** TODO evalute the max speed of blade**/const byte MaxPWMBladeSpeed = 220;const byte stepPWMBladeSpeed=20;  // Incremento/decremento minimo della velocità delle lame.// volatiles are subject to modification by IRQs/**  * volatile unsigned long raintime;  volatile unsigned long rainlast;  volatile unsigned long raininterval;  volatile float rainCurrentHour[60]; // mm di piogga nell'ora corrente durante ciclo di interupt, spezzato in 60 minuti. **///-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=void setup() {  Serial.begin(9600);  int serRetry = 0;  while (!Serial || serRetry < 5) { // wait for serial    serRetry++;    delay(500); //riciclo 5 volte con 500ms di attesa dopo di che forza andare avanti  }  delay(1500);  Serial.println("Robot startup...");//Below all the healty check of sub components PCB  checkRealTimeClock();   // Check Real Time Clock  if(!checkSDCardPCB()) while(true); //Check of SD Card PCB  if(!enableBlades()) while(true); //Activion of Blade motor by H-Bridge    // accendo gli interupts  //interrupts();  //Serial.println("Setup cloud...");  //ArduinoCloud.begin(ArduinoIoTPreferredConnection);  //Qui devo inserire la configurazione per il cloud  Serial.println("Robot online!");}void loop() {  Serial.println("Robot is running...");}